# Fill in ANY ONE of these to enable the AI decision layer (all have free tiers):
#   Groq    — free key at console.groq.com (no card needed)
#   Gemini  — free key at aistudio.google.com/apikey (no card needed)
#   OpenAI  — paid platform.openai.com
# With none set, the app runs fully deterministic and the demo still works.
GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
LLM_MODEL=
AGENT_MODE=llm
